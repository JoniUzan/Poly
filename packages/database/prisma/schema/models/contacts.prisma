// Contact Management Models
//
// This file contains models related to contacts and people management

model Contact {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  phone     String?
  company   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("contacts")
}
